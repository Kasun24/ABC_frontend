<script lang="ts" setup>
/**
 * @description This script sets up the component using Vue's composition API.
 * It imports the Listing component and uses it in the template.
 *
 * @param {Object} props - The Vue component props.
 * @param {Function} emits - The Vue component emits function.
 *
 * @returns {void} - No return value.
 */
import Listing from "@/components/dashboard/users/Listing.vue";
</script>

<template>
  <Listing />
</template>
